## 01-27
- coder 如何演进？手写、代码补全、vibe coding、1人+nAgents、1Agent + nSubAgents
- 如果不需要人工介入，AI 是不是会写机器码

## 1月26号
- 这几天看到各种文章在讨论 mcp、skills、agent 概念的区别，今天就被 clawdbot 霸屏了
- 关于 calwdbot，除了各种吹嘘，还有人偏严谨，觉得风险极高，而且出了很多段子（一觉醒来，钱没了，婚离了。。。），有风险是不是说明有潜力？
- 就像 n 年前电梯刚出来、汽车刚出来、自动驾驶刚出来，人比较希望自己有掌控感
- 说实话，我现在还没搞懂 manus、cowork 解决了啥问题，好像都没实现 compuer use
## 1月25号
这两天找了不少多 agent 的文章，想要享受一下包工头的感觉，启动多个 agent：builder、rewiewer、code-simpler、bug-fixer 等，让这些 agent 自己干活，我等着验收就行了。

研究了两天发现效果并没有那么理想。假如涉及到多 agent，除非是 claude code 自动创建的subagent，否则你需要保证几个 agent 的协同，agent 如何协作、如何协调节奏、如何通信等。

如果每个任务都能快速完成，就不需要多 agent 了。比如 builder 写完代码，我才需要 reviewer，那我告诉 agent 这个流程不久行了吗？为什么还要多 agent。

后来想了想，目前 agent 也不能自主完成编码，即使有 reviewer agent 等，还是需要一定程度的人工介入，之前想用多 agent 解放双手似乎是一场梦，突然就被打回了现实。

未来的开发流程是怎样的呢？AI 能介入多少？

## 1月24号
看到宝玉老师提到 chatbot 和 agent，还挺有启发的。chatbot 就是聊天机器人，根据已有的知识来回答；而 agent 则会决策和调用工具，老解决你的目标问题。

## 1月23号
同事：rules 和 skills 的区别是什么？我写了个 skills，感觉放到 rules 里也是一样的；

我说：嗯，都是提示词。还没见过问 rules vs skills 的，一般好像都是问 mcp vs skills。从能力上看，貌似确实没啥区别，都是提示词，但是从大模型的视角看还是有区别的，skills 支持渐进式加载、执行脚本的，我觉得它是可复用的经验；

同事：rules 也能执行脚本，rules 也能加载一部分；

这个同事的说法肯定是有问题的，现在 rules 貌似被滥用了，不仅是约束大模型，还可以当成知识库。

## 1月22号
最近后端开始使用 Spec Coding，通过公司内的 block 平台搭建流水线，据说效果非常好，有的同学已经几个月不写代码了，主要是 vibe coding；

我们团队内的 AI 小团队也在尝试 spec coding，主 R 认为：这个技术并没有给他提效，可能对不熟悉项目的人才有用。

前端框架杂乱，公司内又各种封装，目前有两大框架 MSC 和 MRN，这种扔给 AI，它会很懵逼；而后端不过是 Spring 框架，目前已经很成熟，而且后端强制需要有单测，再有就是后端主要聚焦在逻辑上，单测又能保证逻辑的正确，这种对于非常 AI 友好，这么看后端落地 spec coding 的成本非常低。

# 2025 年
这一年写代码主要使用 cursor，完全交给它肯定不行，vibe coding 也需要很多人工介入，主要体现在问题排查和验收上。

对于问题排查，cursor 并不能自主排查修复，即使安装了 playwright 貌似也不行，有些问题需要让它添加日志，然后我手动复制 log 给它排查；
对于验收，有很多不符合预期，比如 UI、交互，甚至有些逻辑也是不符合预期的。
